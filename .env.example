# Create .env from this example file

# Override any of these locally. compose.yml has sane fallbacks if this file is absent.
WP_PORT=8080

DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=rootpassword
